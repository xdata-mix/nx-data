name: refresh-olatv

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */4 * * *'   # toutes les 4h

concurrency:
  group: refresh-olatv-only
  cancel-in-progress: false

permissions:
  contents: write
  actions: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Installer les dependances
        run: pip install requests pycryptodome

      - name: Probe + catalogue FR (live-cids + fr-channels)
        env:
          OLA_OUT: data/olatv/live-cids.json
          OLA_OUT_CHANS: data/olatv/fr-channels.json
        run: python scripts/olatv/refresh_olatv.py

      - name: Valider et pousser si modifie
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/olatv/live-cids.json data/olatv/fr-channels.json || true
          if git diff --cached --quiet; then
            echo "Aucun changement OlaTV."
          else
            git commit -m "chore(olatv): refresh cids + catalogue FR [skip ci]"
            for i in 1 2 3 4 5; do
              git pull --rebase origin main \
                && git push origin main \
                && { echo "push OK (essai $i)"; break; }
              echo "push rejete, retry $i"; sleep 6
            done
          fi

      - name: Nettoyage automatique des anciens flux de travail
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          retain_days: 0
          keep_minimum_runs: 5
